apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}-blue
spec:
  selector:
    app: {{ .Values.app.name }}
    version: blue
  ports:
  - port: 80
    targetPort: {{ .Values.app.port }}
