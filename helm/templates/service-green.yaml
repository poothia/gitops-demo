apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}-green
spec:
  selector:
    app: {{ .Values.app.name }}
    version: green
  ports:
  - port: 80
    targetPort: {{ .Values.app.port }}

